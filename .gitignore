# ============================================================
# .gitignore 示例（供参考，非强制）
#
# 用法：如需启用，复制成 .gitignore 即可，再按你的项目情况增删：
#     cp .gitignore.example .gitignore
#
# 主要用来忽略编译产物，避免把它们误提交到参赛仓。
# 其他类型（编辑器临时文件、缓存、本地密钥等）可按需自行添加。
#
# 注意：logs/ 下最终导出的 AI Coding 日志必须提交，不要忽略！
# ============================================================

# Build artifacts
build/
out/
*.o
*.a
*.elf
*.hex
*.map

# 文档流水线中间产物（html -> odt -> docx / pdf，odt 是中间步）
*.odt

# 只交付 v2.67 一份固件，v2.60~v2.66 的七份历史镜像留在本地当退路（每份 27 MB）
image/kid_buddy_gemini-s1_uart0_128Mnand_v2.6[0-6].img
image/kid_buddy_gemini-s1_uart0_128Mnand_v2.6[0-6].img.sha256
